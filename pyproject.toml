[project]
name = "codesage-ai"
version = "0.1.0"
description = "Starter project structure for CodeSage AI"
requires-python = ">=3.10"
dependencies = [
    "fastapi>=0.138.1",
    "uvicorn>=0.49.0",
]
