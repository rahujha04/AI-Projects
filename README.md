# CodeSage AI

CodeSage AI is an AI-powered repository assistant.

It scans repositories.

It chunks files.

It creates embeddings.

It stores vectors.

It retrieves relevant code.

It sends context to an LLM.

The LLM answers questions about the repository.

This project is designed to teach AI Engineering from scratch.